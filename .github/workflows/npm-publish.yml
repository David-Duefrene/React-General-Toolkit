name: npm-publish
on:
    push:
        branches:
            - master
jobs:
    npm-publish:
        name: npm-publish
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@master
            - uses: actions/setup-node@master
              with:
                node-version: 14
                registry-url: https://registry.npmjs.org/dduefrene
            - run: npm build
            - run: npm publish --access public
              env:
                NPM_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
