version: "3.2"

services:
    front_end:
        build: ./
        user: node
        working_dir: /
        volumes:
            - ./:/code
            - ./node_modules
        ports:
            - "3000:3000"
        environment:
            - NODE_ENV=development
        tty: true
        command: npm start
